# Arch package

The package builds Sway Nook for `x86_64` from a release archive verified with SHA-256.

Install `base-devel`, then build and install from this directory:

```sh
makepkg -si
```

The package installs the binary, stylesheet, GPL license, and example configuration under `/usr`. Configure the shortcuts as described in the [main README](../../README.md).
